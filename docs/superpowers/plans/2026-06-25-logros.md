# Sistema de Logros — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistema de logros para Freaky Super Pixel: banner animado (video) al desbloquear en partida + popup scrolleable en Extras con la grilla 2×3 de logros, persistido a nivel perfil.

**Architecture:** Singletons auto-bootstrap (`RuntimeInitializeOnLoadMethod`) al estilo del proyecto. `AchievementManager` mantiene el estado y persiste en JSON. `AchievementCatalog` (tabla estática en código) define los 6 logros y las rutas de sus PNGs/videos en `Resources`. `AchievementTriggers` (componente por nivel) traduce eventos del juego (muertes, daño, cinemática del boss) a desbloqueos. `AchievementBanner` reproduce el `.mp4` del logro con cola. `AchievementsPanelUI` es el popup de Extras (ScrollRect + GridLayoutGroup) en el lobby.

**Tech Stack:** Unity (C#), UnityEngine.Video (`VideoPlayer`), uGUI, `JsonUtility`, ffmpeg (transcodificación de assets).

**Nota sobre verificación:** El proyecto no tiene framework de tests automatizados (estilo existente: UI auto-bootstrap + verificación manual en Play). Cada tarea se verifica con (a) **compilación sin errores** en la consola de Unity y (b) **pruebas manuales en Play** con logs `[Achievement]`. No se agrega test framework (sería scope creep).

---

## File Structure

**Nuevos:**
- `Assets/Resources/Achievements/Sprites/logro_*.png` (6 logros + `logros_contenedor.png`)
- `Assets/Resources/Achievements/Videos/logro_*.mp4` (6 videos, transcodificados de `.mov`)
- `Assets/Scripts/Systems/Achievements/AchievementId.cs` — enum de los 6 logros
- `Assets/Scripts/Systems/Achievements/AchievementCatalog.cs` — tabla estática (def + rutas + metas)
- `Assets/Scripts/Systems/Achievements/AchievementManager.cs` — singleton, persistencia, unlock, contadores
- `Assets/Scripts/Systems/Achievements/AchievementTriggers.cs` — glue por nivel
- `Assets/Scripts/UI/AchievementBanner.cs` — banner de video con cola
- `Assets/Scripts/UI/AchievementsPanelUI.cs` — popup de Extras (grilla scrolleable)

**Modificados:**
- `Assets/Scripts/PlayerHealth.cs` — agregar evento estático `OnPlayerDamaged`
- `Assets/Scripts/UI/BossIntroDirector.cs` — agregar evento estático `OnIntroStarted`
- Escena `Assets/Scenes/Nivel01.unity` — agregar GameObject con `AchievementTriggers`
- Escena `Assets/Scenes/MainMenu.unity` — agregar `AchievementsPanelUI` + estación de Extras que la abre

---

## Task 1: Importar y transcodificar los assets

**Files:**
- Create dir: `Assets/Resources/Achievements/Sprites/`, `Assets/Resources/Achievements/Videos/`
- Source: `C:\Users\juani\Desktop\UI juego\últ ui\Logros`

Mapa de renombrado a ASCII (evita problemas de encoding con `Resources.Load` y la "ñ"):

| Origen | Destino |
|--------|---------|
| `LOGRO_mercadito.png` | `Sprites/logro_mercadito.png` |
| `LOGRO_arañas.png` | `Sprites/logro_aranas.png` |
| `LOGRO_esqueleto.png` | `Sprites/logro_esqueleto.png` |
| `LOGRO_oso.png` | `Sprites/logro_oso.png` |
| `LOGRO_easteregg.png` | `Sprites/logro_easteregg.png` |
| `LOGRO_axel.png` | `Sprites/logro_axel.png` |
| `LOGROS_contenedor.png` | `Sprites/logros_contenedor.png` |
| `logro_nivel1.mov` | `Videos/logro_nivel1.mp4` |
| `logro_arañas.mov` | `Videos/logro_aranas.mp4` |
| `logro_esqueletos.mov` | `Videos/logro_esqueletos.mp4` |
| `logro_oso.mov` | `Videos/logro_oso.mp4` |
| `logro_easteregg.mov` | `Videos/logro_easteregg.mp4` |
| `logro_axel.mov` | `Videos/logro_axel.mp4` |

- [ ] **Step 1: Crear carpetas destino**

```bash
mkdir -p "Assets/Resources/Achievements/Sprites" "Assets/Resources/Achievements/Videos"
```

- [ ] **Step 2: Copiar los PNGs renombrados**

```bash
SRC="/c/Users/juani/Desktop/UI juego/últ ui/Logros"
DST="Assets/Resources/Achievements/Sprites"
cp "$SRC/LOGRO_mercadito.png"  "$DST/logro_mercadito.png"
cp "$SRC/LOGRO_arañas.png"     "$DST/logro_aranas.png"
cp "$SRC/LOGRO_esqueleto.png"  "$DST/logro_esqueleto.png"
cp "$SRC/LOGRO_oso.png"        "$DST/logro_oso.png"
cp "$SRC/LOGRO_easteregg.png"  "$DST/logro_easteregg.png"
cp "$SRC/LOGRO_axel.png"       "$DST/logro_axel.png"
cp "$SRC/LOGROS_contenedor.png" "$DST/logros_contenedor.png"
```

- [ ] **Step 3: Transcodificar cada `.mov` a `.mp4` H.264 + AAC**

ffmpeg ya se usó para el portal. Si `ffmpeg` no está en PATH, usar la ruta absoluta del binario.

```bash
SRC="/c/Users/juani/Desktop/UI juego/últ ui/Logros"
DST="Assets/Resources/Achievements/Videos"
declare -A M=(
  ["logro_nivel1"]="logro_nivel1"
  ["logro_arañas"]="logro_aranas"
  ["logro_esqueletos"]="logro_esqueletos"
  ["logro_oso"]="logro_oso"
  ["logro_easteregg"]="logro_easteregg"
  ["logro_axel"]="logro_axel"
)
for k in "${!M[@]}"; do
  ffmpeg -y -i "$SRC/$k.mov" -c:v libx264 -pix_fmt yuv420p -c:a aac -movflags +faststart "$DST/${M[$k]}.mp4"
done
```

Expected: 6 archivos `.mp4` en `Videos/`, sin errores de ffmpeg.

- [ ] **Step 4: Importar en Unity y configurar import settings**

En el Editor (o vía unity-mcp `Unity_ManageAsset`): que Unity importe la carpeta. Configurar cada PNG de `Sprites/` como **Texture Type = Sprite (2D and UI)**. Los `.mp4` se importan como `VideoClip` por defecto (no tocar).

- [ ] **Step 5: Verificar que cargan por Resources**

En el Editor, ventana → consola limpia tras reimport. Verificación de runtime real va en Task 3 (Step de play).

- [ ] **Step 6: Commit**

```bash
git add Assets/Resources/Achievements
git commit -m "Logros: importar PNGs y videos (mov->mp4) a Resources"
```

---

## Task 2: Enum + catálogo de logros

**Files:**
- Create: `Assets/Scripts/Systems/Achievements/AchievementId.cs`
- Create: `Assets/Scripts/Systems/Achievements/AchievementCatalog.cs`

- [ ] **Step 1: Crear el enum**

`Assets/Scripts/Systems/Achievements/AchievementId.cs`:

```csharp
// Identificadores de los logros. El orden define la grilla del popup (2 columnas x 3 filas).
public enum AchievementId
{
    DeCompras,
    ControlDePlagas,
    SepultureroEnRacha,
    UnOsoWacho,
    ElFamosoEasterEgg,
    AxelElCapo
}
```

- [ ] **Step 2: Crear el catálogo**

`Assets/Scripts/Systems/Achievements/AchievementCatalog.cs`:

```csharp
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Video;

// Definición de un logro. Carga sprite/video de Resources de forma perezosa (lazy).
public class AchievementDef
{
    public AchievementId id;
    public string displayName;
    public string spriteResource;   // ruta relativa dentro de una carpeta Resources/, sin extensión
    public string videoResource;

    private Sprite _sprite;
    private VideoClip _video;

    public Sprite Sprite => _sprite != null ? _sprite : (_sprite = Resources.Load<Sprite>(spriteResource));
    public VideoClip Video => _video != null ? _video : (_video = Resources.Load<VideoClip>(videoResource));
}

// Tabla estática de los 6 logros + metas de los contadores.
public static class AchievementCatalog
{
    public const int SpiderGoal = 10;     // Control de plagas
    public const int SkeletonGoal = 15;   // Sepulturero en racha

    public const string ContainerResource = "Achievements/Sprites/logros_contenedor";

    // El orden ES el orden de la grilla del popup.
    public static readonly AchievementDef[] All =
    {
        new AchievementDef { id = AchievementId.DeCompras,           displayName = "De compras",           spriteResource = "Achievements/Sprites/logro_mercadito", videoResource = "Achievements/Videos/logro_nivel1" },
        new AchievementDef { id = AchievementId.ControlDePlagas,     displayName = "Control de plagas",    spriteResource = "Achievements/Sprites/logro_aranas",    videoResource = "Achievements/Videos/logro_aranas" },
        new AchievementDef { id = AchievementId.SepultureroEnRacha,  displayName = "Sepulturero en racha", spriteResource = "Achievements/Sprites/logro_esqueleto", videoResource = "Achievements/Videos/logro_esqueletos" },
        new AchievementDef { id = AchievementId.UnOsoWacho,          displayName = "Un oso wacho",         spriteResource = "Achievements/Sprites/logro_oso",       videoResource = "Achievements/Videos/logro_oso" },
        new AchievementDef { id = AchievementId.ElFamosoEasterEgg,   displayName = "El famoso easter egg", spriteResource = "Achievements/Sprites/logro_easteregg", videoResource = "Achievements/Videos/logro_easteregg" },
        new AchievementDef { id = AchievementId.AxelElCapo,          displayName = "Axel el capo",         spriteResource = "Achievements/Sprites/logro_axel",      videoResource = "Achievements/Videos/logro_axel" },
    };

    private static Dictionary<AchievementId, AchievementDef> _map;

    public static AchievementDef Get(AchievementId id)
    {
        if (_map == null)
        {
            _map = new Dictionary<AchievementId, AchievementDef>();
            foreach (var d in All) _map[d.id] = d;
        }
        return _map[id];
    }
}
```

- [ ] **Step 3: Verificar compilación**

En Unity: que la consola no muestre errores de compilación. (unity-mcp: `Unity_GetConsoleLogs` filtrando errores → vacío.)

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/Systems/Achievements/AchievementId.cs Assets/Scripts/Systems/Achievements/AchievementCatalog.cs
git commit -m "Logros: enum AchievementId + catalogo estatico"
```

---

## Task 3: AchievementManager (singleton + persistencia)

**Files:**
- Create: `Assets/Scripts/Systems/Achievements/AchievementManager.cs`

- [ ] **Step 1: Crear el manager**

`Assets/Scripts/Systems/Achievements/AchievementManager.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

// Estado global de logros (a nivel perfil, NO por slot de guardado).
// Auto-bootstrap + DontDestroyOnLoad, al estilo del resto del proyecto.
public class AchievementManager : MonoBehaviour
{
    public static AchievementManager Instance { get; private set; }

    // Lo escucha el banner para reproducir el video.
    public event Action<AchievementDef> OnUnlocked;

    private readonly HashSet<AchievementId> _unlocked = new HashSet<AchievementId>();
    private int _spiders;     // contador POR PARTIDA (no se persiste)
    private int _skeletons;   // contador POR PARTIDA (no se persiste)

    private string SavePath => Path.Combine(Application.persistentDataPath, "achievements.json");

    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (Instance != null) return;
        var go = new GameObject("AchievementManager");
        DontDestroyOnLoad(go);
        go.AddComponent<AchievementManager>();
    }

    void Awake()
    {
        if (Instance != null && Instance != this) { Destroy(gameObject); return; }
        Instance = this;
        Load();
    }

    public bool IsUnlocked(AchievementId id) => _unlocked.Contains(id);

    public void Unlock(AchievementId id)
    {
        if (_unlocked.Contains(id)) return;
        _unlocked.Add(id);
        Save();
        var def = AchievementCatalog.Get(id);
        Debug.Log($"[Achievement] Unlocked: {def.displayName}");
        OnUnlocked?.Invoke(def);
    }

    // Reinicia los contadores por partida. Lo llama AchievementTriggers al empezar el nivel.
    public void ResetRunCounters()
    {
        _spiders = 0;
        _skeletons = 0;
    }

    public void AddSpiderKill()
    {
        _spiders++;
        if (_spiders >= AchievementCatalog.SpiderGoal) Unlock(AchievementId.ControlDePlagas);
    }

    public void AddSkeletonKill()
    {
        _skeletons++;
        if (_skeletons >= AchievementCatalog.SkeletonGoal) Unlock(AchievementId.SepultureroEnRacha);
    }

    // --- Persistencia ---
    [Serializable]
    private class SaveBlob { public List<string> unlocked = new List<string>(); }

    private void Load()
    {
        _unlocked.Clear();
        try
        {
            if (File.Exists(SavePath))
            {
                var blob = JsonUtility.FromJson<SaveBlob>(File.ReadAllText(SavePath));
                if (blob?.unlocked != null)
                    foreach (var s in blob.unlocked)
                        if (Enum.TryParse(s, out AchievementId id)) _unlocked.Add(id);
            }
        }
        catch (Exception e) { Debug.LogWarning($"[Achievement] Load failed: {e.Message}"); }
    }

    private void Save()
    {
        try
        {
            var blob = new SaveBlob();
            foreach (var id in _unlocked) blob.unlocked.Add(id.ToString());
            File.WriteAllText(SavePath, JsonUtility.ToJson(blob, true));
        }
        catch (Exception e) { Debug.LogWarning($"[Achievement] Save failed: {e.Message}"); }
    }

    // Helper de debug: borra todo el progreso (para testear). Llamar desde un menú o tecla temporal.
    public void DebugResetAll()
    {
        _unlocked.Clear();
        ResetRunCounters();
        Save();
        Debug.Log("[Achievement] Progreso reseteado.");
    }
}
```

- [ ] **Step 2: Verificar compilación**

Unity consola sin errores.

- [ ] **Step 3: Verificación manual de unlock + persistencia**

Crear temporalmente un script de prueba o usar el menú de debug. Verificación mínima:
1. Entrar a Play en cualquier escena.
2. En la consola C# (o un botón temporal) ejecutar:
   `AchievementManager.Instance.Unlock(AchievementId.DeCompras);`
3. Confirmar log `[Achievement] Unlocked: De compras`.
4. Verificar que existe el archivo `%userprofile%/AppData/LocalLow/<Company>/<Product>/achievements.json` con `"DeCompras"`.
5. Salir y volver a entrar a Play → `AchievementManager.Instance.IsUnlocked(AchievementId.DeCompras)` devuelve `true`.

Expected: el JSON persiste el id y se relee al reiniciar.

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/Systems/Achievements/AchievementManager.cs
git commit -m "Logros: AchievementManager con persistencia JSON global"
```

---

## Task 4: Eventos estáticos en PlayerHealth y BossIntroDirector

Necesarios para enganchar el no-hit (Axel) y la cinemática del boss (oso) sin acoplar fuerte.

**Files:**
- Modify: `Assets/Scripts/PlayerHealth.cs`
- Modify: `Assets/Scripts/UI/BossIntroDirector.cs`

- [ ] **Step 1: Agregar evento de daño en PlayerHealth**

En `Assets/Scripts/PlayerHealth.cs`, agregar el evento estático junto a los campos (después de `public int currentHealth;`):

```csharp
    // Se dispara cuando el jugador recibe daño real (>0). Lo usa el sistema de logros (no-hit).
    public static event System.Action OnPlayerDamaged;
```

Dentro de `TakeDamage(int damage)`, después de la línea `currentHealth -= damage;` y su clamp, agregar la notificación. El método queda así en esa zona:

```csharp
        currentHealth -= damage;

        if (currentHealth < 0)
            currentHealth = 0;

        if (damage > 0)
            OnPlayerDamaged?.Invoke();

        if (healthSlider != null)
            healthSlider.value = currentHealth;
```

> Nota: la tecla `H` de debug (`TakeDamage(25)`) también dispara el evento; está bien para testear, en juego real no se usa.

- [ ] **Step 2: Agregar evento de inicio de cinemática en BossIntroDirector**

En `Assets/Scripts/UI/BossIntroDirector.cs`, agregar el evento estático como campo de la clase (debajo de `private BossController currentBoss;`):

```csharp
    // Se dispara cuando arranca la cinemática del boss. Lo usa el logro "Un oso wacho".
    public static event System.Action OnIntroStarted;
```

Y en `PlayIntro(BossController boss)`, dispararlo al inicio:

```csharp
    public void PlayIntro(BossController boss)
    {
        currentBoss = boss;
        OnIntroStarted?.Invoke();
        StartCoroutine(IntroSequence());
    }
```

- [ ] **Step 3: Verificar compilación**

Unity consola sin errores.

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/PlayerHealth.cs Assets/Scripts/UI/BossIntroDirector.cs
git commit -m "Logros: eventos estaticos OnPlayerDamaged y OnIntroStarted"
```

---

## Task 5: AchievementTriggers (glue por nivel) + colocar en Nivel01

**Files:**
- Create: `Assets/Scripts/Systems/Achievements/AchievementTriggers.cs`
- Modify: escena `Assets/Scenes/Nivel01.unity`

- [ ] **Step 1: Crear el componente de triggers**

`Assets/Scripts/Systems/Achievements/AchievementTriggers.cs`:

```csharp
using UnityEngine;

// Glue por nivel: traduce eventos del juego a desbloqueos. Colocar UN GameObject con
// este componente en cada escena de nivel (Nivel01). No requiere referencias en el Inspector.
public class AchievementTriggers : MonoBehaviour
{
    private bool _tookDamage;

    void Start()
    {
        var mgr = AchievementManager.Instance;
        if (mgr == null) return;

        mgr.ResetRunCounters();   // contadores arañas/esqueletos son POR PARTIDA
        _tookDamage = false;

        // "De compras": bienvenida al entrar al Mercadito.
        mgr.Unlock(AchievementId.DeCompras);
    }

    void OnEnable()
    {
        EnemyHealth.OnAnyEnemyDied       += HandleEnemyDied;
        PlayerHealth.OnPlayerDamaged     += HandlePlayerDamaged;
        BossIntroDirector.OnIntroStarted += HandleBossIntro;
        BossBearHealth.OnBossDied        += HandleBossDied;
    }

    void OnDisable()
    {
        EnemyHealth.OnAnyEnemyDied       -= HandleEnemyDied;
        PlayerHealth.OnPlayerDamaged     -= HandlePlayerDamaged;
        BossIntroDirector.OnIntroStarted -= HandleBossIntro;
        BossBearHealth.OnBossDied        -= HandleBossDied;
    }

    // Arañas vs esqueletos: la araña tiene SpiderAI; el resto de enemigos comunes (no boss)
    // se cuentan como esqueletos.
    private void HandleEnemyDied(EnemyHealth eh)
    {
        if (eh == null) return;
        var mgr = AchievementManager.Instance;
        if (mgr == null) return;

        if (eh.GetComponent<SpiderAI>() != null)
            mgr.AddSpiderKill();
        else if (eh.data != null && !eh.data.isBoss)
            mgr.AddSkeletonKill();
    }

    private void HandlePlayerDamaged() => _tookDamage = true;

    private void HandleBossIntro()
        => AchievementManager.Instance?.Unlock(AchievementId.UnOsoWacho);

    // El stage se completa al morir el oso. Si no recibiste daño en toda la corrida -> Axel el capo.
    private void HandleBossDied(BossBearHealth boss)
    {
        if (!_tookDamage)
            AchievementManager.Instance?.Unlock(AchievementId.AxelElCapo);
    }
}
```

> **A verificar al ejecutar:** confirmar la firma de `BossBearHealth.OnBossDied` (en `BossDefeatPortal` se usa como `Action<BossBearHealth>`; si difiere, ajustar el handler). Confirmar también que las arañas tienen componente `EnemyHealth` con `EnemyData` asignado (si murieran por otra vía sin disparar `OnAnyEnemyDied`, enganchar `SpiderAI.Die` aparte).

- [ ] **Step 2: Verificar compilación**

Unity consola sin errores.

- [ ] **Step 3: Colocar el GameObject en Nivel01**

Abrir `Assets/Scenes/Nivel01.unity`. Crear un GameObject vacío llamado `AchievementTriggers` y agregarle el componente `AchievementTriggers`. Guardar la escena.
(unity-mcp: `Unity_ManageGameObject` create + add component sobre la escena Nivel01.)

- [ ] **Step 4: Verificación manual en Play (Nivel01)**

1. Borrar progreso previo: en Play, `AchievementManager.Instance.DebugResetAll()`.
2. Entrar a Nivel01 → debe loguear `[Achievement] Unlocked: De compras`.
3. Matar 10 arañas → `[Achievement] Unlocked: Control de plagas`.
4. Matar 15 esqueletos → `[Achievement] Unlocked: Sepulturero en racha`.
5. Llegar a la cinemática del boss → `[Achievement] Unlocked: Un oso wacho`.
6. Matar al boss sin haber tocado `H` ni recibido golpes → `[Achievement] Unlocked: Axel el capo`.
   (Si recibís daño en la corrida, NO debe desbloquearse.)

Expected: cada log aparece en su condición. (El banner visual se prueba en Task 6.)

- [ ] **Step 5: Commit**

```bash
git add Assets/Scripts/Systems/Achievements/AchievementTriggers.cs "Assets/Scenes/Nivel01.unity"
git commit -m "Logros: AchievementTriggers (glue por nivel) en Nivel01"
```

---

## Task 6: AchievementBanner (video con cola)

**Files:**
- Create: `Assets/Scripts/UI/AchievementBanner.cs`

- [ ] **Step 1: Crear el banner**

`Assets/Scripts/UI/AchievementBanner.cs`:

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Video;

// Banner in-game: al desbloquear un logro reproduce su video (.mp4) arriba en pantalla.
// Auto-bootstrap + DontDestroyOnLoad + cola (si caen dos juntos, van de a uno).
public class AchievementBanner : MonoBehaviour
{
    private static AchievementBanner _instance;

    private VideoPlayer _vp;
    private AudioSource _audio;
    private RawImage _raw;
    private CanvasGroup _group;
    private RenderTexture _rt;

    private readonly Queue<AchievementDef> _queue = new Queue<AchievementDef>();
    private bool _playing;
    private bool _videoFinished;

    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (_instance != null) return;
        var go = new GameObject("AchievementBanner");
        DontDestroyOnLoad(go);
        _instance = go.AddComponent<AchievementBanner>();
    }

    void Awake()
    {
        BuildUI();
        // El manager bootstrappea en BeforeSceneLoad igual que este; si ya existe, suscribir.
        if (AchievementManager.Instance != null)
            AchievementManager.Instance.OnUnlocked += Enqueue;
        else
            StartCoroutine(SubscribeWhenReady());
    }

    private IEnumerator SubscribeWhenReady()
    {
        while (AchievementManager.Instance == null) yield return null;
        AchievementManager.Instance.OnUnlocked += Enqueue;
    }

    void OnDestroy()
    {
        if (AchievementManager.Instance != null)
            AchievementManager.Instance.OnUnlocked -= Enqueue;
        if (_rt != null) _rt.Release();
    }

    private void BuildUI()
    {
        var canvas = gameObject.AddComponent<Canvas>();
        canvas.renderMode = RenderMode.ScreenSpaceOverlay;
        canvas.sortingOrder = 1200;   // por encima del HUD

        var scaler = gameObject.AddComponent<CanvasScaler>();
        scaler.uiScaleMode = CanvasScaler.ScaleMode.ScaleWithScreenSize;
        scaler.referenceResolution = new Vector2(1920, 1080);
        scaler.matchWidthOrHeight = 1f;

        gameObject.AddComponent<GraphicRaycaster>();

        _group = gameObject.AddComponent<CanvasGroup>();
        _group.alpha = 0f;
        _group.blocksRaycasts = false;
        _group.interactable = false;

        var imgGo = new GameObject("BannerVideo", typeof(RectTransform));
        imgGo.transform.SetParent(transform, false);
        _raw = imgGo.AddComponent<RawImage>();
        var rt = _raw.rectTransform;
        rt.anchorMin = rt.anchorMax = new Vector2(0.5f, 1f);
        rt.pivot = new Vector2(0.5f, 1f);
        rt.anchoredPosition = new Vector2(0f, -40f);
        rt.sizeDelta = new Vector2(800f, 225f);   // ajustar al aspect real del video

        _rt = new RenderTexture(1280, 360, 0);
        _raw.texture = _rt;

        _vp = gameObject.AddComponent<VideoPlayer>();
        _vp.playOnAwake = false;
        _vp.isLooping = false;
        _vp.renderMode = VideoRenderMode.RenderTexture;
        _vp.targetTexture = _rt;

        _audio = gameObject.AddComponent<AudioSource>();
        _vp.audioOutputMode = VideoAudioOutputMode.AudioSource;
        _vp.SetTargetAudioSource(0, _audio);

        _vp.loopPointReached += OnVideoEnd;
    }

    private void Enqueue(AchievementDef def)
    {
        if (def == null) return;
        _queue.Enqueue(def);
        if (!_playing) StartCoroutine(PlayLoop());
    }

    private IEnumerator PlayLoop()
    {
        _playing = true;
        while (_queue.Count > 0)
        {
            var def = _queue.Dequeue();
            var clip = def.Video;
            if (clip == null) { Debug.LogWarning($"[Achievement] Sin video para {def.displayName}"); continue; }

            _vp.clip = clip;
            _vp.EnableAudioTrack(0, true);

            bool prepared = false;
            void OnPrep(VideoPlayer v) => prepared = true;
            _vp.prepareCompleted += OnPrep;
            _vp.Prepare();
            float to = 5f;
            while (!prepared && to > 0f) { to -= Time.unscaledDeltaTime; yield return null; }
            _vp.prepareCompleted -= OnPrep;

            _videoFinished = false;
            _group.alpha = 1f;
            _vp.Play();

            // Usar tiempo real: la cinemática del boss congela Time.timeScale.
            float dur = 4f;
            if (clip.frameRate > 0) dur = (float)(clip.frameCount / clip.frameRate);
            float elapsed = 0f;
            while (!_videoFinished && elapsed < dur + 1.5f)
            { elapsed += Time.unscaledDeltaTime; yield return null; }

            _group.alpha = 0f;
            yield return new WaitForSecondsRealtime(0.3f);
        }
        _playing = false;
    }

    private void OnVideoEnd(VideoPlayer vp) => _videoFinished = true;
}
```

- [ ] **Step 2: Verificar compilación**

Unity consola sin errores.

- [ ] **Step 3: Verificación manual en Play**

1. Entrar a Nivel01 (o cualquier escena en Play) tras `DebugResetAll()`.
2. Disparar un unlock: `AchievementManager.Instance.Unlock(AchievementId.ControlDePlagas);`
3. Debe aparecer el video del logro arriba en pantalla, reproducirse con audio, y desaparecer.
4. Disparar dos seguidos → se muestran de a uno (cola).
5. Verificar que durante la cinemática del boss (Time.timeScale=0) el banner igual se reproduce.

Expected: el `.mp4` se ve y se escucha; la cola no pisa banners.

> Si el video se ve estirado, ajustar `rt.sizeDelta` al aspect real del `.mov`.

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/UI/AchievementBanner.cs
git commit -m "Logros: AchievementBanner (video con cola)"
```

---

## Task 7: AchievementsPanelUI (popup de Extras) + estación en el lobby

**Files:**
- Create: `Assets/Scripts/UI/AchievementsPanelUI.cs`
- Modify: escena `Assets/Scenes/MainMenu.unity`

- [ ] **Step 1: Crear el popup**

`Assets/Scripts/UI/AchievementsPanelUI.cs`. Construye la UI por código (estilo del proyecto): fondo = `logros_contenedor`, dentro un ScrollRect con `GridLayoutGroup` de 2 columnas; cada celda es el PNG del logro (desbloqueado a color, bloqueado oscurecido). Se abre desde una `InteractionStation` (`onActivate` → `Open()`), congela a Emilio vía `LobbyModal`.

```csharp
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;
using UnityEngine.InputSystem;

// Popup de Extras (lobby): grilla scrolleable 2x3 con los 6 logros sobre el contenedor.
// Componente de escena (MainMenu). Asignar Open() al onActivate de la estación de Extras.
public class AchievementsPanelUI : MonoBehaviour
{
    [Header("Layout grilla (calibrar sobre el arte del contenedor)")]
    [SerializeField] private Vector2 cellSize = new Vector2(360f, 150f);
    [SerializeField] private Vector2 spacing  = new Vector2(40f, 30f);
    [SerializeField] private RectOffset padding;   // si queda null se usa default en Build

    private Canvas _canvas;
    private CanvasGroup _group;
    private RectTransform _content;
    private bool _open;
    private bool _built;
    private GameObject _firstSelectable;
    private float _armTime;

    private readonly List<(AchievementId id, Image img)> _cells = new List<(AchievementId, Image)>();

    void Awake()
    {
        BuildUI();
        SetVisible(false);
    }

    public void Open()
    {
        if (_open) return;
        _open = true;
        RefreshCells();
        SetVisible(true);
        LobbyModal.Open();
        _armTime = Time.unscaledTime + 0.3f;   // evita que la misma tecla la cierre al instante
        if (EventSystem.current != null) EventSystem.current.SetSelectedGameObject(_firstSelectable);
    }

    public void Close()
    {
        if (!_open) return;
        _open = false;
        SetVisible(false);
        LobbyModal.Close();
    }

    void Update()
    {
        if (!_open || Time.unscaledTime < _armTime) return;

        Gamepad gp = Gamepad.current;
        bool cancel = Input.GetKeyDown(KeyCode.Escape)
                   || (gp != null && (gp.buttonEast.wasPressedThisFrame || gp.startButton.wasPressedThisFrame));
        if (cancel) Close();
    }

    private void SetVisible(bool v)
    {
        if (_group == null) return;
        _group.alpha = v ? 1f : 0f;
        _group.blocksRaycasts = v;
        _group.interactable = v;
        _canvas.gameObject.SetActive(true); // el canvas queda activo; controlamos por alpha/raycast
    }

    // Actualiza cada celda según el estado de desbloqueo (color vs oscurecido).
    private void RefreshCells()
    {
        var mgr = AchievementManager.Instance;
        foreach (var (id, img) in _cells)
        {
            bool unlocked = mgr != null && mgr.IsUnlocked(id);
            img.color = unlocked ? Color.white : new Color(0.18f, 0.18f, 0.18f, 0.85f);
        }
    }

    private void BuildUI()
    {
        if (_built) return;
        _built = true;
        if (padding == null) padding = new RectOffset(40, 40, 40, 40);

        var go = new GameObject("AchievementsCanvas");
        go.transform.SetParent(transform, false);
        _canvas = go.AddComponent<Canvas>();
        _canvas.renderMode = RenderMode.ScreenSpaceOverlay;
        _canvas.sortingOrder = 1150;
        var scaler = go.AddComponent<CanvasScaler>();
        scaler.uiScaleMode = CanvasScaler.ScaleMode.ScaleWithScreenSize;
        scaler.referenceResolution = new Vector2(1920, 1080);
        scaler.matchWidthOrHeight = 0.5f;
        go.AddComponent<GraphicRaycaster>();
        _group = go.AddComponent<CanvasGroup>();

        // Backdrop oscuro a pantalla completa.
        var backdrop = NewImage("Backdrop", go.transform);
        StretchFull(backdrop.rectTransform);
        backdrop.color = new Color(0f, 0f, 0f, 0.7f);

        // Contenedor (la hoja rasgada con "LOGROS").
        var cont = NewImage("Container", go.transform);
        var contSprite = Resources.Load<Sprite>(AchievementCatalog.ContainerResource);
        var crt = cont.rectTransform;
        crt.anchorMin = crt.anchorMax = new Vector2(0.5f, 0.5f);
        crt.anchoredPosition = Vector2.zero;
        float ch = 820f;
        float cw = contSprite != null ? ch * (contSprite.rect.width / contSprite.rect.height) : 1300f;
        crt.sizeDelta = new Vector2(cw, ch);
        if (contSprite != null) { cont.sprite = contSprite; cont.preserveAspect = true; cont.color = Color.white; }
        else cont.color = new Color(0.15f, 0.12f, 0.2f, 1f);

        // Viewport del ScrollRect (recorta el área de la grilla, dentro del contenedor,
        // dejando margen para el título "LOGROS" de arriba).
        var viewportGo = new GameObject("Viewport", typeof(RectTransform), typeof(RectMask2D), typeof(Image));
        viewportGo.transform.SetParent(cont.transform, false);
        var vrt = (RectTransform)viewportGo.transform;
        vrt.anchorMin = new Vector2(0.06f, 0.05f);
        vrt.anchorMax = new Vector2(0.94f, 0.78f);   // 0.78 deja espacio arriba para el título
        vrt.offsetMin = Vector2.zero; vrt.offsetMax = Vector2.zero;
        viewportGo.GetComponent<Image>().color = new Color(0, 0, 0, 0); // invisible, solo para raycast/mask

        var scroll = cont.gameObject.AddComponent<ScrollRect>();
        scroll.viewport = vrt;
        scroll.horizontal = false;
        scroll.vertical = true;
        scroll.movementType = ScrollRect.MovementType.Clamped;
        scroll.scrollSensitivity = 30f;

        // Content con GridLayoutGroup (2 columnas) + ContentSizeFitter.
        var contentGo = new GameObject("Content", typeof(RectTransform));
        contentGo.transform.SetParent(viewportGo.transform, false);
        _content = (RectTransform)contentGo.transform;
        _content.anchorMin = new Vector2(0f, 1f);
        _content.anchorMax = new Vector2(1f, 1f);
        _content.pivot = new Vector2(0.5f, 1f);
        _content.anchoredPosition = Vector2.zero;
        scroll.content = _content;

        var grid = contentGo.AddComponent<GridLayoutGroup>();
        grid.constraint = GridLayoutGroup.Constraint.FixedColumnCount;
        grid.constraintCount = 2;
        grid.cellSize = cellSize;
        grid.spacing = spacing;
        grid.padding = padding;
        grid.childAlignment = TextAnchor.UpperCenter;

        var fitter = contentGo.AddComponent<ContentSizeFitter>();
        fitter.verticalFit = ContentSizeFitter.FitMode.PreferredSize;

        // Una celda por logro, en el orden del catálogo.
        foreach (var def in AchievementCatalog.All)
        {
            var cell = NewImage($"Cell_{def.id}", contentGo.transform);
            cell.sprite = def.Sprite;
            cell.preserveAspect = true;
            _cells.Add((def.id, cell));
            if (_firstSelectable == null) _firstSelectable = cell.gameObject;
        }
    }

    private static Image NewImage(string name, Transform parent)
    {
        var go = new GameObject(name, typeof(RectTransform));
        go.transform.SetParent(parent, false);
        return go.AddComponent<Image>();
    }

    private static void StretchFull(RectTransform rt)
    {
        rt.anchorMin = Vector2.zero; rt.anchorMax = Vector2.one;
        rt.offsetMin = Vector2.zero; rt.offsetMax = Vector2.zero;
    }
}
```

- [ ] **Step 2: Verificar compilación**

Unity consola sin errores.

- [ ] **Step 3: Colocar el panel y la estación en MainMenu**

Abrir `Assets/Scenes/MainMenu.unity`:
1. Crear GameObject `AchievementsPanel` con el componente `AchievementsPanelUI`.
2. Para la estación de Extras: reusar/crear una `InteractionStation` (igual que las de Controles/Dificultad). En su `onActivate` (UnityEvent), agregar el target `AchievementsPanel` → método `AchievementsPanelUI.Open`.
3. Guardar la escena.

> Si ya existe una estación/botón de "Extras" en el lobby, solo enganchar su `onActivate` a `Open()`.

- [ ] **Step 4: Verificación manual en Play (MainMenu)**

1. Con algunos logros desbloqueados (y otros no) vía `DebugResetAll()` + `Unlock(...)`.
2. Acercarse a la estación de Extras y activarla (E / X).
3. Debe abrirse el popup con el contenedor de fondo y la grilla 2×3.
4. Logros desbloqueados → a color; bloqueados → oscurecidos.
5. Scrollear (rueda / stick) funciona; Emilio queda congelado (LobbyModal).
6. Esc / B (gamepad) cierra el popup y descongela a Emilio.

Expected: el popup coincide con el layout del arte; bloqueados se ven apagados.

> Calibración: ajustar `cellSize`, `spacing`, `padding` y los anchors del Viewport en el Inspector hasta que la grilla caiga sobre los slots del contenedor.

- [ ] **Step 5: Commit**

```bash
git add Assets/Scripts/UI/AchievementsPanelUI.cs "Assets/Scenes/MainMenu.unity"
git commit -m "Logros: popup de Extras (grilla scrolleable 2x3)"
```

---

## Self-Review (cobertura del spec)

- **6 logros** → Task 2 (catálogo) + Task 5 (triggers). ✓
- **De compras** (entrar a Mercadito) → Task 5 Start. ✓
- **Control de plagas / Sepulturero** (10/15, por partida) → Task 3 contadores + `ResetRunCounters` + Task 5. ✓
- **Un oso wacho** (cinemática boss) → Task 4 `OnIntroStarted` + Task 5. ✓
- **El famoso easter egg** (sin trigger) → presente en catálogo (Task 2), aparece en popup siempre bloqueado, ningún handler lo desbloquea. ✓
- **Axel el capo** (no-hit al completar) → Task 4 `OnPlayerDamaged` + `BossBearHealth.OnBossDied` en Task 5. ✓
- **Banner animado con videos + cola** → Task 6. ✓
- **Persistencia global (JSON, no por slot)** → Task 3. ✓
- **Popup de Extras: grilla 2×3 scrolleable sobre el contenedor, bloqueado/desbloqueado, sin panel de detalle** → Task 7. ✓
- **Transcodificación .mov→.mp4 + assets a Resources** → Task 1. ✓

Sin placeholders. Firmas consistentes entre tasks (`Unlock`, `IsUnlocked`, `AddSpiderKill`, `AddSkeletonKill`, `ResetRunCounters`, `OnUnlocked`, `AchievementDef`, `AchievementCatalog.All/Get`). Dos puntos marcados "a verificar al ejecutar" en Task 5 (firma de `OnBossDied`, vía de muerte de arañas).
